/**
 * Provider-backed adapter sets.
 *
 * Empty by design — no provider is integrated. See `README.md` in this
 * directory for the contract an adapter set has to fit and the rules it has to
 * keep, and `src/services/registry.ts` for where one is registered.
 *
 * Nothing outside this directory may name a provider.
 */
export {};
