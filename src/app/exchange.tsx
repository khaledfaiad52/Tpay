import { PhasePlaceholder } from '@/components/navigation';

export default function ExchangeScreen() {
  return (
    <PhasePlaceholder
      title="Exchange"
      phase="Phase 2"
      summary="Convert between your currency accounts with a live rate, the fee and the exact amount you receive."
    />
  );
}
