import { PhasePlaceholder } from '@/components/navigation';

export default function TransactionsScreen() {
  return (
    <PhasePlaceholder
      title="Transactions"
      phase="Phase 2"
      summary="Every salary, transfer, card payment, deposit, exchange and fee, filterable and searchable."
    />
  );
}
