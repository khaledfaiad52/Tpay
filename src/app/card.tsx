import { PhasePlaceholder } from '@/components/navigation';

export default function CardScreen() {
  return (
    <PhasePlaceholder
      title="TPay Card"
      phase="Phase 5B"
      summary="Card details, status, freeze and unfreeze, card transactions and spending — all drawing on your one wallet balance."
    />
  );
}
