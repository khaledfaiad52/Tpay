import { PhasePlaceholder } from '@/components/navigation';

export default function EmployerScreen() {
  return (
    <PhasePlaceholder
      title="My employer"
      phase="Phase 4"
      summary="Your employer, job title, employment status and type, start date, country, contract status and salary."
    />
  );
}
