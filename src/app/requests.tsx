import { PhasePlaceholder } from '@/components/navigation';

export default function RequestsScreen() {
  return (
    <PhasePlaceholder
      title="Requests"
      phase="Phase 4"
      summary="Employment letters, payslips, insurance support, bank account support, expense reimbursement and HR support — tracked from submitted to completed."
    />
  );
}
