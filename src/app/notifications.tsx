import { PhasePlaceholder } from '@/components/navigation';

export default function NotificationsScreen() {
  return (
    <PhasePlaceholder
      title="Notifications"
      phase="Phase 5"
      summary="Money movements, salary alerts, card activity and requests from your employer."
    />
  );
}
