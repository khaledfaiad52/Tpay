import { PhasePlaceholder } from '@/components/navigation';

export default function SalaryScreen() {
  return (
    <PhasePlaceholder
      title="Salary"
      phase="Phase 4"
      summary="Next salary, salary history and the full breakdown — gross, net, taxes, social insurance, benefits and deductions — plus payslips."
    />
  );
}
